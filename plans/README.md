# Plans

Generated by `/improve` against commit `a682fa7`. Focus: reduce over-engineering, YAGNI, and boilerplate.

## Execution order

| # | Plan | Category | Effort | Status | Depends on |
|---|---|---|---|---|---|
| 001 | [Delete dead prop-types.min.js](001-delete-dead-prop-types.md) | Delete | S | TODO | — |
| 002 | [Extract `_run_async` helper](002-run-async-helper.md) | Shrink | S | TODO | — |
| 003 | [Drop SUGGESTIONS: fallback](003-drop-parse-ai-suggestions-fallback.md) | YAGNI | S | TODO | — |

All three are independent — run in any order or in parallel.

## Dependency graph

```
001 ──┐
002 ──┤ (independent)
003 ──┘
```

## Considered and rejected

- **if-elif routing dispatch in Handler** — 35+ branches across do_GET/do_POST. A dict dispatch would be slightly cleaner but the current pattern is readable, explicit, and not a bug source. Not worth it.
- **`_do_recreate` as inner closure** — 120-line function inside apply_self_update. Moving to module level is readability-only; zero behavior change. The self-update system is intentionally self-contained. Not worth it.
- **`_parse_ai_response` attempt 2 (JSON scan)** — Legitimate: real LLMs prepend reasoning prose before JSON. Keep.
- **MOCK data in index.html** — Documented intentional fallback for SE demo mode and offline use. Keep.
